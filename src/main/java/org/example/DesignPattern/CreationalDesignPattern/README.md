Creational Pattern focus on object creation mechanism aiming to optimize the creation process while ensuring flexibility.  

The goal is to abstract the instantiation process to make system more flexible and reusable.

It prevents the tight coupling between the code and object creation logic, simplifying the management of new object creation. In application where different types of documents(PDF, Excel, Word) are created instead of using new keyword a Factory Pattern can centralize the object creation.

